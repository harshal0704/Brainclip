# ── Brainclip Colab Runtime — Pinned Dependencies ──
# These versions are tested together on Colab T4 (Python 3.10+).
# Install order matters: torch → vllm-omni → everything else.

# --- Core ML ---
torch==2.6.0
torchaudio==2.6.0
numpy==2.1.3
scipy==1.15.2

# --- TTS Engines (at least one required) ---
vllm-omni
fish-speech

# --- Server ---
fastapi==0.115.8
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.6
nest-asyncio==1.6.0

# --- Audio & Transcription ---
soundfile==0.13.1
faster-whisper==1.1.1

# --- HuggingFace ---
huggingface-hub>=0.28.1
transformers==4.49.0
accelerate==1.2.1
sentencepiece==0.2.0
einops==0.8.1

# --- Utilities ---
requests==2.32.3
loguru==0.7.3
